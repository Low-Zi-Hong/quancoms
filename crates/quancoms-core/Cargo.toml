[package]
name = "quancoms-core"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0"
rayon = "1.11.0"
sysinfo = "0.38.4"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "gate_benchmarks"
harness = false
